CREATE TABLE [dbo].[5eCharacterMoney]
(
	[CharacterId] INT NOT NULL PRIMARY KEY, 
    [CP] INT NOT NULL, 
    [SP] INT NOT NULL, 
    [EP] INT NOT NULL, 
    [GP] INT NOT NULL, 
    [PP] INT NOT NULL
)
